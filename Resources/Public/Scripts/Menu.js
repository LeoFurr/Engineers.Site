/* Open when someone clicks on the span element */
function openNav() {
    document.getElementById("skyLaunchNav").style.width = "100%";
}

/* Close when someone clicks on the "X" symbol inside the overlay */
function closeNav() {
    document.getElementById("skyLaunchNav").style.width = "0";
}